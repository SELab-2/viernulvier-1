name: Build and Publish (Production)

on:
  push:
    branches:
      - "main"

permissions:
  contents: write  # Required for creating releases
  packages: write

jobs:
  build-and-push:
    runs-on: [self-hosted, linux, ARM64]
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push Images
        run: |
          docker build -t ghcr.io/selab-2/viernulvier-1-frontend:main -t ghcr.io/selab-2/viernulvier-1-frontend:${{ github.sha }} -f ./frontend/Dockerfile .
          docker build -t ghcr.io/selab-2/viernulvier-1-backend:main -t ghcr.io/selab-2/viernulvier-1-backend:${{ github.sha }} -f ./backend/Dockerfile .
          docker push ghcr.io/selab-2/viernulvier-1-frontend --all-tags
          docker push ghcr.io/selab-2/viernulvier-1-backend --all-tags

  deploy:
    needs: build-and-push
    runs-on: [self-hosted, linux, ARM64]
    steps:
      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: viernulvier-archive.be
          username: selab2
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 2002
          script: |
            cd ~/viernulvier-server
            docker compose pull
            docker compose up -d --remove-orphans

  create-release:
    needs: deploy
    runs-on: [self-hosted, linux, ARM64]
    steps:
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: release-${{ github.sha }}
          name: Release ${{ github.sha }}
          body: |
            Automated release from main branch.
            Commit: ${{ github.sha }}
          draft: false
          prerelease: false